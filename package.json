{
  "name": "prep4iiser-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^10.12.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
    // <-- CHECK HERE: NO trailing comma after "react-dom" if it's the last item
  },
  "devDependencies": {
    "@types/react": "^18.2.66",
    "vite": "^5.2.0"
    // <-- CHECK HERE: NO trailing comma after "vite" if it's the last item
  }
}
